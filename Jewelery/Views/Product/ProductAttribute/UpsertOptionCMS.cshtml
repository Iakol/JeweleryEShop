@*@model Jewelery.Models.Product_model.ProductAttributes

@{
    Layout = "~/Views/Shared/_LayoutCSM.cshtml";
}


<div class="d-flex justify-content-center">
    <label for="upsertform">
        @if (Model.Atribute_id == 0)
        {
            <span>Створити Атрибут</span>
        }
        else
        {
            <span>Редагувати Атрибут</span>
        }
    </label>
    
    <form id="upsertform" method="post">
       
        <div>
            <input type="hidden" placeholder="Ід категорії" asp-for="Atribute_id" />
            <span asp-validation-for="Atribute_id"></span>
        </div>
        <div>
            <input type="number" step="0.01" placeholder="Розмір" asp-for="Size" />
            <span asp-validation-for="Size"></span>
        </div>
        <div>
            <input type="number" step="0.01" placeholder="Доодаткова ціна" asp-for="PriceAdjustment" />
            <span asp-validation-for="PriceAdjustment"></span>
        </div>

        <div>
            <button type="submit">
                @if (Model.Atribute_id == 0)
                {
                    <span>Створити</span>
                }
                else
                {
                    <span>Редагувати</span>
                }
            </button>
        </div>
    </form>
</div>*@